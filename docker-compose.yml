services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: demoacssiondocker-app
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=
    restart: unless-stopped
